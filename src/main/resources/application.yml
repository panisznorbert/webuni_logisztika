spring:
  profiles:
    active: -smart

  thymeleaf:
    cache: false

  mvc:
    format:
      date-time: yyyy-MM-dd HH:mm:ss

  datasource:
    url: jdbc:postgresql://localhost:5432/webuni
    username: postgres
    password: postgres

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create